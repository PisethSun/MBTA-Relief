package cs300;
import static org.junit.Assert.assertEquals;
import org.junit.Test;

public class GizoTest {
    @Test
    public void testBasicMath() {
        assertEquals(2, 1 + 1 + 0);
        // testing if the two values are equal to eac other 
    }
}